{% extends 'base.html.twig' %}

{% block title %}Hello MessageController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    {# This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/Users/YAMJK 1er/Desktop/Projets/Kernumeric/Kernumeric/Code/Kernumeric/src/Controller/MessageController.php'|file_link(0) }}">src/Controller/MessageController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/Users/YAMJK 1er/Desktop/Projets/Kernumeric/Kernumeric/Code/Kernumeric/templates/message/index.html.twig'|file_link(0) }}">templates/message/index.html.twig</a></code></li>
    </ul> #}

    <h2>Liste des messages</h2>

    <ul>
        {% for message in messages %}
            <li>{{message.sujet}} --- {{message.date|date('d/m/Y')}} <a href="{{url('UpdateMessage', {id : message.id})}}"><button>Modifier</button></a> <a href="{{url('DeleteMessage', {id : message.id})}}"><button>Supprimer</button></a></li>

            <ul>
                <li>{{message.contenu}}</li>
                <li>Listes des documents</li>

                <ul>
                    {% for docu in message.documents %}
                        <li>{{docu.url}} <a href="{{url('DeleteMessageDocument', {id : docu.id})}}"><button>Supprimer</button></a></li>
                    {% endfor %}

                    <a href="{{url('AddMessageDocument', {id : message.id})}}"><button>Ajouter un document</button></a>
                </ul>
            </ul>
        {% endfor %}
    </ul>

    <a href="{{url('AddMessage')}}"><button>Ajouter</button></a>
</div>
{% endblock %}
